<%= form_for @record, url: record_attachment_path(@record, @record.class.default_datastream) do |f| %>

  <%= render 'attachments/files_form/upload', dsid: @record.class.default_datastream, record: @record %>
<% end %>

