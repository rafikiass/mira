<%= form_for @record, url: record_attachment_path(@record, @record.class.original_file_datastreams.first) do |f| %>

  <%= render 'attachments/files_form/upload', dsid: @record.class.original_file_datastreams.first, record: @record %>
<% end %>

